# C# Calculator

Для запуска выполните:
```bash
git clone https://github.com/Nick18899/C_sharp_calculator.git
cd C_sharp_calculator
dotnet run
```

Введите 2 числа, выберите операцию (`+` `-` `*` `/`), результат распечатается в консоли. Напишите `q` для выхода.
