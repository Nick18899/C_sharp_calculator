# C# Calculator

```bash
git clone https://github.com/Nick18899/C_sharp_calculator.git
cd C_sharp_calculator
dotnet run
```

Enter two numbers, choose operation (`+` `-` `*` `/`), get result. Type `q` to exit.
